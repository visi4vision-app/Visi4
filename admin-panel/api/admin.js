export const adminActions = {
  banUser: async (uid) => console.log("User banni:", uid),
  stopLive: async (liveId) => console.log("Live stoppé:", liveId),
  addGift: async (gift) => console.log("Gift ajouté:", gift),
};
